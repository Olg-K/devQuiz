/**
 * #1 Value type properties in prototype
 * 
 * console.info(bar.count) prints 1
 * console.info(buz.count) prints 0
 *
 * Value types are searched using prototype chain.
 * When assigned, the instance itself is being used
 *
*/
